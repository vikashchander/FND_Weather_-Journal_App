{
  "name": "fnd-weather-app",
  "version": "1.0.0",
  "description": "'udacity 3rd project's, learn asynchronous web app that uses Web API and user data to dynamically update the UI '",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vikash chander", 
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  }
}
